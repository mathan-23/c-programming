#include <stdio.h>

int main() {
    int n;
    scanf("%d", &n);  // Array size

    int arr[n];
    for (int i = 0; i < n; i++) {
        scanf("%d", &arr[i]);   
    }

    for (int i = 0; i < 2 * n; i++) {
        printf("%d ", arr[i % n]); 
    }

    printf("\n");
    return 0;
}
